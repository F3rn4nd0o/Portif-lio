import { styled } from "..";

export const Container1 = styled('div', {
  display: 'flex',
  flexDirection: 'rows',
  alignItems: 'center',
  justifyContent: 'center',
})

export const Text = styled('text', {
  fontSize: '18px',
})