import { createTheme } from "..";

export const light = createTheme({
  colors: {
    primary: '#fff',
    secondary: '',
    tertiary: '#00c16c',
    fourth: '#002e34',

    text: '#000'
  },
})